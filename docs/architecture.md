# Architecture

The system uses a client-server design. The UE client uploads inputs and downloads result packages. The server owns provider selection, processing, storage, and manifest generation.

First-stage development runs locally on Windows with mock providers only.
